name: Update WCA Data

on: 
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check WCA Export
        run: |
          curl  https://www.worldcubeassociation.org/api/v0/export/public